const nome = document.getElementById("nome").value;
const data = document.getElementById("data do evento").value;
const convidados = document.getElementById("convidados").value;

addEventListener("submit", (e) => {
    e.preventDefault();
}); 